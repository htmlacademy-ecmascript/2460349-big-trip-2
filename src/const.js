const DATE_FORMAT = {
  MONTH_DAY: 'MMM D',
  HOURS: 'HH:MM',
  FULL_DATE_TIME: 'MM/DD/YYYY HH:mm',
};

const POINT_COUNT = 4;

const FilterType = {
  EVERYTHING: 'everything',
  FUTURE: 'future',
  PRESENT: 'present',
  PAST: 'past',
};

const Mode = {
  DEFAULT: 'DEFAULT',
  EDITING: 'EDITING',
};


export {DATE_FORMAT, POINT_COUNT, FilterType, Mode};
